<div>
        <div class="row">
                <div class="col s2"></div>
                <div class="col s8 left-align">
                        {{#if saved}}
                        {{#each saved}}
                        <div class="card-panel">
                                <img src={{img}}>
                                <h5>{{title}}</h5>
                                <a href={{link}} class="waves-effect waves-light btn #ef9a9a red lighten-3"><i
                                                class="material-icons left" data-state="false"
                                                id={{title}}>add_circle</i>Read More </a>

                                <a class="waves-effect waves-light btn #ef9a9a red lighten-3"><i
                                                class="material-icons left" data-state="true"
                                                data-id={{_id}}>add_circle</i>Save This Article
                                </a>
                                <a class="waves-effect waves-light btn #ef9a9a red lighten-3"><i
                                                class="material-icons left" data-id={{_id}}>add_circle</i>Add/View Notes
                                </a>
                        </div>
                        {{/each}}
                        {{else}}
                        <h3> {{nothing}} </h3>
                        <a href='/scrape' class="waves-effect waves-light btn #ef9a9a red lighten-3"><i
                                        class="material-icons left">add_circle</i>{{nothing}}</a>
                        {{/if}}
                </div>
        </div>